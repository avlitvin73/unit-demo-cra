#there will be some workflow

name: ${{ GITHUB_ACTOR }} is running tests on ${{ GITHUB_REPOSITORY_NAME }}

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

